import StyledLoading, { LoadingAnimation } from './StyledLoading';

const Loading = () => {
  return (
    <StyledLoading>
      <LoadingAnimation />
    </StyledLoading>
  );
};

export default Loading;
